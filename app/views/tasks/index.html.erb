<body>
  <h1>Task List</h1>

  <ul class="task-list">
    <% @tasks.each do |task| %>
      <li><%= link_to task.title, task_path(task)%>
      <%= link_to "🗑",
      task_path(task),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
      %>
      </li>
    <% end %>
  </ul>
  <button class="btn btn-primary"><%=link_to "Add a new task", new_task_path%></button>
</body>
