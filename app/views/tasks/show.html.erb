<h2><%= "#{@task.title}" %></h2>

<p><%= "Description: #{@task.details}"%></p>
<p><%=if @task.completed == false
  "Task not yet completed :("
else "Task completed ✅"
end %></p>

<button class="btn btn-primary"><%=link_to "Back to tasks", tasks_path%></button>

<button class="btn btn-danger"><%=link_to "Edit task", edit_task_path(@task)%></button>
